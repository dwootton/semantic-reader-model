name: Publish Banana Bread demo

on:
  push:
    branches: [main]
    paths:
      - 'inspector/index.html'
      - 'inspector/style.css'
      - 'inspector/*.mjs'
      - 'inspector/build_data.py'
      - 'examples/banana-bread/**'
      - 'scripts/build_pages_demo.py'
      - 'scripts/check_staged_files.py'
      - 'tests/test_pages_demo.py'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.12'
      - name: Test the public export boundary
        run: python3 -m unittest discover -s tests -p test_pages_demo.py -q
      - name: Build reviewed static files
        run: python3 scripts/build_pages_demo.py --destination .pages-dist
      - uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: .pages-dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
